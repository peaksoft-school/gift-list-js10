// import { SearchSelect } from './components/UI/SearchSelect'

export function App() {
   // const { values, handleChange, handleReset } = useFormik({
   //    initialValues: {
   //       state: '',
   //       category: '',
   //       subCategory: '',
   //       country: '',
   //       search: '',
   //    },
   // }) // formik тин зависимостун кочуруш керек
   return (
      <div className="App">
         {/* <SearchSelect
            values={values}
            handleChange={handleChange}
            handleReset={handleReset}
            variant="select" //вариантты бербесек обычный инпут болот
         /> */}
      </div>
   )
}
